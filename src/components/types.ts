export type ButtonAppearance = 'primary' | 'secondary' | 'danger' | 'success' | 'warning';
export type ButtonSize = 'small' | 'medium' | 'large';
export type Targets = '_blank' | '_self' | '_parent' | '_top';

export const TagTypes = {
	button: 'button',
	a: 'a',
};
